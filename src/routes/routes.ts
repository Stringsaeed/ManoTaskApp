// here we only add the routes names
export enum Route {
  // * Note: I changed the home screen name to discovery as it make more sense
  // * the same screen should be unified for all the users.
  // ! Why? As the user discover the products
  Discovery = 'Discovery',
  Product = 'Product',
}
